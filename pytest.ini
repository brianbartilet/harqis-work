# pytest.ini
[pytest]
python_files = test_*.py unit_tests_*.py
env_files = .env/apps.env

# Ignore the workflows folder
addopts = --ignore=workflows/

# Register custom markers here to silence warnings and enable -m filtering
markers =
    smoke: quick smoke tests that run fast and validate critical paths
    sanity: basic end-to-end checks (lightweight)