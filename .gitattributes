# Shell scripts must keep LF endings — a CRLF shebang ("#!/usr/bin/env bash\r")
# fails on Linux. Pins all tracked *.sh (host scripts + edge/ device bootstraps)
# regardless of the committer's core.autocrlf setting.
*.sh text eol=lf
